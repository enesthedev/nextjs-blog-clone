module.exports = {
    /*
    webpack: (config, { buildId, dev, isServer, defaultLoaders, webpack, paths }) => {
        config.module.rules.push({
            test: /\.pdf$/,
            use: {
                loader: "url-loader"
            }
        })

        return config
    },
     */
}